'use strict';

module.exports = ({ resize, background, crop, embed, max, min, toFormat,
extract, trim, flatten, extend, negate, rotate, flip, flop, blur, sharpen, gamma,
grayscale, greyscale, normalize, normalise, convolve, threshold, toColourspace,
toColorspace, ignoreAspectRatio, withMetadata, withoutEnlargement }) => ({
  resize,
  background,
  crop,
  embed,
  max,
  min,
  toFormat,
  extract,
  trim,
  flatten,
  extend,
  negate,
  rotate,
  flip,
  flop,
  blur,
  sharpen,
  gamma,
  grayscale,
  greyscale,
  normalize,
  normalise,
  convolve,
  threshold,
  toColourspace,
  toColorspace,
  ignoreAspectRatio,
  withMetadata,
  withoutEnlargement
});
